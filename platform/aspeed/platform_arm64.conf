#!/bin/sh
# Implementation of installation behavior for Aspeed AST2700 BMC platform

echo "Preparing for Aspeed AST2700 installation..."

# SONiC filesystem definitions (must match installer/install.sh)
DOCKERFS_DIR=docker
FILESYSTEM_DOCKERFS=dockerfs.tar.gz
FILESYSTEM_SQUASHFS=fs.squashfs

# FIT image configuration
fit_fname="/boot/sonic_arm64.fit"

# Device tree blob configuration
# The DTB is selected at boot time by U-Boot based on hardware detection
# U-Boot sets the 'bootconf' environment variable to the appropriate configuration name
# This supports multiple vendors with a single image containing multiple DTBs in a FIT image
# Example: bootconf="ast2700-evb" or bootconf="nexthop-b27-r0"
# The bootconf variable is used to select the FIT configuration: #conf-${bootconf}
# Note: bootconf contains the base name without .dtb extension

# Platform detection
# During ONIE or SONiC installation, $onie_platform is already set by install.sh from machine.conf
# During build (install_env=build), machine.conf doesn't exist yet, use a default
if [ "$install_env" = "build" ]; then
    onie_platform="arm64-aspeed_ast2700_evb-r0"
    echo "Build mode: Using default platform $onie_platform"
else
    # Try to load device-specific configuration
    if [ -n "$onie_platform" ] && [ -r "platforms/$onie_platform" ]; then
        . "platforms/$onie_platform"
    else
        echo "Warning: No device-specific configuration found for $onie_platform"
        echo "         Will use default configuration for Aspeed AST2700"
    fi
fi

# Set defaults when invoked from 'build' install env OR
# if not specified in installer.conf
# Override these values in device-specific installer.conf 
# AND here if hand crafting a disk image to burn for the
# first install
CONSOLE_DEV=${CONSOLE_DEV:-12}
CONSOLE_SPEED=${CONSOLE_SPEED:-115200}
EARLYCON=${EARLYCON:-"earlycon=uart8250,mmio32,0x14c33b00"}
VAR_LOG=${VAR_LOG:-${VAR_LOG_SIZE:-128}}  # 128MB for /var/log tmpfs

# Construct console device name
CONSOLE_PORT="ttyS${CONSOLE_DEV}"

echo "Installing SONiC from ${install_env:-unknown} on Platform $onie_platform"

# AST2700 platform default configurations
disk_interface="mmc"
mmc_bus="mmc0:0001" 

# Device tree and FIT image configuration
# DTB is embedded in FIT image, selected by bootconf U-Boot variable
# FIT configuration is selected by U-Boot bootconf variable
# bootconf can be set by either U-Boot based on hardware detection or
# from device/<vendor>/<platform>/installer.conf
# Example: bootconf="ast2700-evb" selects #conf-ast2700-evb from FIT image

# U-Boot memory addresses for AST2700
# Based on actual hardware memory map from bdinfo:
# - RAM: 0x400000000 - 0x47e000000 (2016 MB)
# - Reserved: 0x41b800000-0x423ffffff (136 MB), 0x42c000000-0x431bfffff (92 MB)
# - Safe addresses avoid reserved regions
# See platform/aspeed/AST2700-MEMORY-MAP.md for details
#
# Memory Layout (works for both production and installation initramfs):
#   0x403000000: Kernel (~10 MB)
#   0x440000000: Initramfs (192 MB allocated for decompression)
#                - Production initramfs: 37.4 MB compressed, 160 MB uncompressed
#                - Installation initramfs: 3-5 MB compressed, ~10 MB uncompressed
#   0x44C000000: DTB (~50 KB) - after initramfs (0x440000000 + 192 MB)
#
# IMPORTANT: Initramfs is compressed (zstd) but kernel decompresses IN PLACE
# Need space for UNCOMPRESSED size (160 MB), not compressed size (37 MB)!
#
# Memory layout (avoiding reserved regions 0x41b800000-0x423ffffff and 0x42c000000-0x431bfffff):
# fit_addr: where FIT image is loaded (temporary, ~100 MB, after reserved region 2)
# kernel_addr: where kernel is extracted from FIT (U-Boot default loadaddr)
# initrd_addr: where initrd is extracted from FIT (after reserved regions, 192 MB allocated)
# fdt_addr: where device tree blob is extracted from FIT (after initramfs space)
fit_addr=0x432000000
kernel_addr=0x403000000
initrd_addr=0x440000000
fdt_addr=0x44C000000

# U-Boot environment location
# AST2700 typically stores U-Boot env in SPI flash (MTD)
FW_ENV_DEFAULT='/dev/mtd1 0x0 0x20000 0x10000'

# Extra kernel command line for GRUB bootloader configuration
# This is appended to the kernel command line in the generated grub.cfg
ONIE_PLATFORM_EXTRA_CMDLINE_LINUX="$EARLYCON console=$CONSOLE_PORT,${CONSOLE_SPEED}n8"

# Get eMMC block device
# AST2700 EVB has eMMC at /dev/mmcblk0 (mmc0:0001)
get_install_device()
{
    # In build mode, there's no hardware - skip device detection
    if [ "$install_env" = "build" ]; then
        return 0
    fi

    if [ ! -z "$mmc_bus" ]; then
        for i in 0 1 2 ; do
            if $(ls -l /sys/block/mmcblk$i/device 2>/dev/null | grep -q "$mmc_bus") ; then
                echo "/dev/mmcblk$i"
                blk_dev=/dev/mmcblk$i
                disk_interface="mmc"
                echo "Selected eMMC device: $blk_dev"
                return
            fi
        done
    fi

    echo "Warning: eMMC not found. Will try installing on the same disk as ONIE."
}

get_install_device

# Run the new image's sonic-fw-env-config.sh (loop-mounted from its squashfs),
# not the old rootfs's copy which may predate AST2700 fixes.
configure_uboot_env() {
    echo "Configuring U-Boot environment access..."

    new_squashfs="$demo_mnt/$image_dir/$FILESYSTEM_SQUASHFS"
    if [ ! -f "$new_squashfs" ]; then
        echo "ERROR: new image squashfs not found at $new_squashfs"
        return 1
    fi

    loop_mnt=$(mktemp -d) || {
        echo "ERROR: mktemp -d failed."
        return 1
    }

    if ! mount -o ro,loop -t squashfs "$new_squashfs" "$loop_mnt"; then
        echo "ERROR: failed to mount $new_squashfs on $loop_mnt"
        rmdir "$loop_mnt" 2>/dev/null
        return 1
    fi

    cue_rc=0
    cue_sh=""
    for p in "$loop_mnt/usr/bin/sonic-fw-env-config.sh" \
             "$loop_mnt/sbin/sonic-fw-env-config.sh"; do
        if [ -x "$p" ]; then
            cue_sh="$p"
            break
        fi
    done

    if [ -z "$cue_sh" ]; then
        echo "ERROR: sonic-fw-env-config.sh not found in $new_squashfs"
        cue_rc=1
    elif ! onie_platform="$onie_platform" "$cue_sh"; then
        echo "ERROR: $cue_sh failed."
        cue_rc=1
    fi

    umount "$loop_mnt" 2>/dev/null || umount -l "$loop_mnt" 2>/dev/null || true
    rmdir "$loop_mnt" 2>/dev/null || true

    [ "$cue_rc" -ne 0 ] && return "$cue_rc"

    if fw_printenv bootcmd >/dev/null 2>&1; then
        echo "U-Boot environment is accessible"
        return 0
    fi

    echo "ERROR: fw_printenv cannot read U-Boot environment."
    cat /etc/fw_env.config 2>/dev/null | sed 's/^/  /'
    return 1
}

# Prepare U-Boot boot menu
prepare_boot_menu() {
    echo "Sync up cache ..."
    sync
    echo "Setting up U-Boot environment for Aspeed AST2700..."

    # Configure U-Boot environment access first
    # This function detects MTD, device tree, or uses eMMC default
    configure_uboot_env

    # U-Boot uses relative paths (without leading /)
    # image_dir from installer.sh is already in correct format (e.g., "image-version")
    fit_name=${image_dir}${fit_fname}

    # Get partition UUID for the installation target (single SONiC-OS partition)
    uuid=$(blkid | grep "$demo_volume_label" | sed -ne 's/.* UUID=\"\([^"]*\)\".*/\1/p')
    demo_part=$(sgdisk -p $blk_dev | grep -e "$demo_volume_label" | awk '{print $1}')

    # Standard SONiC approach: Single partition with multiple image directories
    # sonic_image_1 = current/new image
    # sonic_image_2 = previous image (for rollback)

    if [ "$install_env" = "onie" ]; then
        FW_ARG="-f"
    fi
    if [ "$install_env" = "onie" ] || [ "$install_env" = "build" ]; then
        # First installation - initialize boot configuration
        echo "First installation: Setting up boot configuration for image directory: $image_dir"
        fw_setenv  ${FW_ARG} image_dir_old ""
        fw_setenv  ${FW_ARG} fit_name_old ""
        fw_setenv  ${FW_ARG} sonic_version_2 "None"
        fw_setenv  ${FW_ARG} linuxargs_old ""
    else
        # Upgrade scenario - save current image as "old" (image_2)
        echo "Upgrade installation: Saving current image as backup"

        CURR_SONIC_IMAGE="$(sonic-installer list 2>/dev/null | grep "Current: " | cut -f2 -d' ')"
        FIRST_SONIC_IMAGE="$(fw_printenv -n sonic_version_1 2>/dev/null || true)"
        SECOND_SONIC_IMAGE="$(fw_printenv -n sonic_version_2 2>/dev/null || true)"

        echo "Current running image: $CURR_SONIC_IMAGE"
        echo "Image slot 1 (sonic_version_1): $FIRST_SONIC_IMAGE"
        echo "Image slot 2 (sonic_version_2): $SECOND_SONIC_IMAGE"

        # Guard against fw_printenv-broken state: without a valid current image,
        # empty==empty below would match slot 1 and wipe slot 2 with empty values.
        # Requiring the comparand to be non-empty closes the same hole per-branch.
        if [ -z "$CURR_SONIC_IMAGE" ]; then
            echo "WARNING: current SONiC image unknown; skipping slot-2 backup."
        elif [ -n "$FIRST_SONIC_IMAGE" ] && [ "$CURR_SONIC_IMAGE" = "$FIRST_SONIC_IMAGE" ]; then
            # Currently running image_1, save it to image_2 slot
            echo "Current image matches slot 1, saving to slot 2 as backup"
            image_dir_old=$(fw_printenv -n image_dir 2>/dev/null || true)
            fit_name_old=$(fw_printenv -n fit_name 2>/dev/null || true)
            sonic_version_2="$CURR_SONIC_IMAGE"
            linuxargs_old=$(fw_printenv -n linuxargs 2>/dev/null || true)

            echo "Saving current image to slot 2: $sonic_version_2"
            fw_setenv  ${FW_ARG} image_dir_old "$image_dir_old"
            fw_setenv  ${FW_ARG} fit_name_old "$fit_name_old"
            fw_setenv  ${FW_ARG} sonic_version_2 "$sonic_version_2"
            fw_setenv  ${FW_ARG} linuxargs_old "$linuxargs_old"
        elif [ -n "$SECOND_SONIC_IMAGE" ] && [ "$CURR_SONIC_IMAGE" = "$SECOND_SONIC_IMAGE" ]; then
            # Currently running image_2, keep it in slot 2
            echo "Current image matches slot 2, keeping it as backup"
            # No need to update image_2 variables, they're already correct
        else
            # Current image doesn't match either slot - this shouldn't happen in normal operation
            # But we'll save it anyway to be safe
            echo "WARNING: Current image doesn't match any slot, saving to slot 2 anyway"

            # Try to find the current image directory from /proc/cmdline (most reliable)
            CURR_IMAGE_DIR=$(cat /proc/cmdline | sed -n 's/^.*loop=\/*\(image-[^/]*\)\/.*$/\1/p')

            if [ -z "$CURR_IMAGE_DIR" ]; then
                # Fallback: derive from sonic-installer list
                CURR_IMAGE_DIR=$(echo "$CURR_SONIC_IMAGE" | sed 's/SONiC-OS-/image-/')
            fi

            if [ -n "$CURR_IMAGE_DIR" ] && [ -d "/host/$CURR_IMAGE_DIR" ]; then
                image_dir_old="$CURR_IMAGE_DIR"
                fit_name_old="$CURR_IMAGE_DIR/boot/sonic_arm64.fit"
                sonic_version_2="$CURR_SONIC_IMAGE"

                # Reconstruct linuxargs from current boot
                linuxargs_old="console=$CONSOLE_PORT,${CONSOLE_SPEED}n8 $EARLYCON loopfstype=squashfs loop=$CURR_IMAGE_DIR/$FILESYSTEM_SQUASHFS varlog_size=$VAR_LOG logs_inram=on"

                echo "Saving current image to slot 2: $sonic_version_2"
                fw_setenv ${FW_ARG} image_dir_old "$image_dir_old"
                fw_setenv ${FW_ARG} fit_name_old "$fit_name_old"
                fw_setenv ${FW_ARG} sonic_version_2 "$sonic_version_2"
                fw_setenv ${FW_ARG} linuxargs_old "$linuxargs_old"
            else
                echo "ERROR: Cannot find current image directory, skipping backup"
            fi
        fi
    fi

    # Set current image variables (image_1)
    echo "Setting sonic_version_1 to: $demo_volume_revision_label"

    fw_setenv  ${FW_ARG} image_dir "$image_dir"
    fw_setenv  ${FW_ARG} fit_name "$fit_name"
    fw_setenv ${FW_ARG}  sonic_version_1 "$demo_volume_revision_label"

    # Boot menu with instructions
    BORDER='echo "===================================================";'
    TITLE='echo "SONiC Boot Menu";'
    BOOT1='echo "To boot $sonic_version_1";echo "  type: run sonic_image_1";echo "  at the U-Boot prompt after interrupting U-Boot when it says";echo "  \"Hit any key to stop autoboot:\" during boot";echo;'
    BOOT2='echo "To boot $sonic_version_2";echo "  type: run sonic_image_2";echo "  at the U-Boot prompt after interrupting U-Boot when it says";echo "  \"Hit any key to stop autoboot:\" during boot";echo;'
    FOOTER='echo "===================================================";'
    fw_setenv  ${FW_ARG} print_menu "$BORDER $TITLE $BORDER $BOOT1 $BOOT2 $FOOTER"

    # Boot arguments for current image (linuxargs contains loop path)
    fw_setenv ${FW_ARG} linuxargs "console=$CONSOLE_PORT,${CONSOLE_SPEED}n8 $EARLYCON loopfstype=squashfs loop=$image_dir/$FILESYSTEM_SQUASHFS varlog_size=$VAR_LOG logs_inram=on"

    # Boot commands - always load from partition 1 (SONiC-OS)
    DISK_LOAD="mmc dev 0; ext4load $disk_interface 0:$demo_part \${loadaddr} \${fit_name}"
    BOOTARGS='setenv bootargs root=UUID='$uuid' rw rootwait panic=1 ${linuxargs}'
    SONIC_BOOT_CMD='run sonic_bootargs; run sonic_boot_load; bootm $loadaddr#conf-$bootconf'

    # Old image boot commands
    sonic_bootargs_old='setenv bootargs root=UUID='$uuid' rw rootwait panic=1 ${linuxargs_old}'
    DISK_LOAD_OLD="mmc dev 0; ext4load $disk_interface 0:$demo_part \${loadaddr} \${fit_name_old}"
    SONIC_BOOT_CMD_OLD='run sonic_bootargs_old; run sonic_boot_load_old; bootm $loadaddr#conf-$bootconf'

    # Set U-Boot variables
    fw_setenv ${FW_ARG}  sonic_boot_load "$DISK_LOAD"
    fw_setenv ${FW_ARG}  sonic_boot_load_old "$DISK_LOAD_OLD"
    fw_setenv ${FW_ARG}  sonic_bootargs "$BOOTARGS"
    fw_setenv ${FW_ARG}  sonic_bootargs_old "$sonic_bootargs_old"
    fw_setenv ${FW_ARG}  sonic_image_1 "$SONIC_BOOT_CMD"
    fw_setenv ${FW_ARG}  sonic_image_2 "$SONIC_BOOT_CMD_OLD"
    fw_setenv ${FW_ARG}  boot_next 'run sonic_image_1'
    fw_setenv ${FW_ARG}  bootcmd 'run print_menu; test -n "$boot_once" && setenv do_boot_once "$boot_once" && setenv boot_once "" && saveenv && run do_boot_once; run boot_next'

    # Set memory addresses (used by manual boot if needed)
    fw_setenv ${FW_ARG}  loadaddr $fit_addr
    fw_setenv ${FW_ARG}  kernel_addr $kernel_addr
    fw_setenv ${FW_ARG}  fdt_addr $fdt_addr
    fw_setenv ${FW_ARG}  initrd_addr $initrd_addr

    # bootconf is overrided with device/<vendor>/<platform>/installer.conf
    if [ -n "${bootconf}" ]; then
        fw_setenv ${FW_ARG}  bootconf $bootconf
    fi

    # Display installation summary
    echo ""
    echo "========================================="
    echo "SONiC Installation Complete"
    echo "========================================="
    echo "Installed to: Partition $demo_part ($demo_volume_label)"
    echo "Image: $demo_volume_revision_label"
    echo ""

    if [ "$install_env" != "onie" ] && [ "$install_env" != "build" ]; then
        # Get current U-Boot variables for display
        UBOOT_VERSION_1=$(fw_printenv -n sonic_version_1 2>/dev/null || echo "None")
        UBOOT_VERSION_2=$(fw_printenv -n sonic_version_2 2>/dev/null || echo "None")

        echo "Dual-boot configuration:"
        echo "  - Image 1 (sonic_version_1): $UBOOT_VERSION_1"
        echo "  - Image 2 (sonic_version_2): $UBOOT_VERSION_2"
        echo "  - Next boot: Image 1"
        echo ""
        echo "To switch to backup image, run:"
        echo "  sonic-installer set-default $UBOOT_VERSION_2"
        echo ""
    fi

    echo "U-Boot configuration updated successfully"
    echo "========================================="
}

# Configure bootloader menu
bootloader_menu_config() {
    # In "build" mode (creating raw image), we can't access U-Boot environment
    # Skip U-Boot configuration in this case
    if [ "$install_env" = "build" ]; then
        echo "Build mode: Skipping U-Boot environment configuration"
        echo "U-Boot environment will be configured on first boot"

        # Sync to flush all writes
        sync

        # Convert demo_mnt to absolute path before changing directory
        # (demo_mnt is a relative path like "build_raw_image_mnt")
        demo_mnt_abs=$(realpath "$demo_mnt")

        # Change back to root directory BEFORE unmounting (critical for cleanup)
        cd /

        # Unmount the raw image using absolute path
        umount "$demo_mnt_abs" || {
            echo "Warning: Failed to unmount $demo_mnt_abs"
            # Try lazy unmount as fallback
            umount -l "$demo_mnt_abs" || true
        }

        echo "Installed SONiC base image $demo_volume_label successfully"
        return 0
    fi

    # Update U-Boot Environment for ONIE or SONiC installation
    prepare_boot_menu
}

